package com.example.todoapp.model;

public class DateHeader {
    public String title;
    public boolean isExpanded;

    public DateHeader(String title, boolean isExpanded) {
        this.title = title;
        this.isExpanded = isExpanded;
    }
}